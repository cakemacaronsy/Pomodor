# Red Strong 番茄鐘 v1

TL;DR：這是依照 2026-06-18 09:43 截圖整理出的 `red strong version`。它保留強番茄視覺、Sean header、左側今日進度與時間設定、中央大圓計時器、右側專注清單。

## 版本內容

| 項目 | 說明 |
| --- | --- |
| 版本 | `pomodoro-red-strong_v1` |
| 入口 | `index.html` |
| 樣式 | `styles.css` |
| 功能 | `app.js` |
| 圖示 | `assets/tomato.svg` |

## 使用方式

1. 直接打開 `versions/pomodoro-red-strong_v1/index.html`。
2. 或在 `versions/pomodoro-red-strong_v1/` 執行：

```bash
python3 -m http.server 4175
```

然後開啟 `http://127.0.0.1:4175/`。

## 檢查點

| 檢查 | 結果 |
| --- | --- |
| 可獨立開啟 | 是 |
| 不覆蓋 green / red 舊版本 | 是 |
| 專屬本機儲存 key | 是 |
| 支援新增與勾選專注事項 | 是 |

